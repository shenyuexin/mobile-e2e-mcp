---
phase: 01-capability-verification-productization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - packages/adapter-maestro/src/harness-config.ts
  - packages/adapter-maestro/test/harness-config.test.ts
autonomous: true
requirements:
  - CAP-01
  - CFG-01
  - CFG-02
user_setup: []
must_haves:
  truths:
    - Canonical harness and framework-matrix inputs are treated as repository-owned runtime inputs rather than ignored local-only files.
    - Missing harness config no longer silently falls back to an implicit default for validation-oriented runner profiles.
  artifacts:
    - .gitignore
    - packages/adapter-maestro/src/harness-config.ts
    - packages/adapter-maestro/test/harness-config.test.ts
  key_links:
    - Harness config loading behavior matches the tracked config policy documented by the repository.
---

<objective>
Harden the repository's canonical harness and framework-matrix inputs so clean clones, contributors, and CI all evaluate the same baseline.

Purpose: Remove silent local-config drift from the current runtime contract.
Output: Tracked config policy in `.gitignore`, stricter harness-config loading behavior, and tests covering the new behavior.
</objective>

<execution_context>
@.planning/README.md
@.planning/templates/SUMMARY-TEMPLATE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capability-verification-productization/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@packages/adapter-maestro/src/harness-config.ts
@scripts/validate-phase3-samples.ts
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stop treating canonical harness and matrix files as ignored local-only artifacts</name>
  <files>.gitignore</files>
  <read_first>.gitignore, configs/harness/sample-harness.yaml, configs/matrices/framework-profile-matrix.md, .planning/codebase/CONCERNS.md</read_first>
  <action>Update `.gitignore` so the repository no longer ignores the canonical runtime inputs under `configs/harness/` and `configs/matrices/`. Preserve ignore rules for genuinely generated artifacts, but make `configs/harness/sample-harness.yaml` and `configs/matrices/framework-profile-matrix.md` trackable by git without requiring local exceptions.</action>
  <verify>rg -n "configs/harness|configs/matrices" .gitignore</verify>
  <acceptance_criteria>
    - `.gitignore` does not contain a broad ignore rule that blocks `configs/harness/`
    - `.gitignore` does not contain a broad ignore rule that blocks `configs/matrices/`
  </acceptance_criteria>
  <done>Canonical config paths are trackable and no longer implied to be local-only.</done>
</task>

<task type="auto">
  <name>Task 2: Replace silent harness fallback with explicit missing-config behavior for repo baselines</name>
  <files>packages/adapter-maestro/src/harness-config.ts</files>
  <read_first>packages/adapter-maestro/src/harness-config.ts, scripts/validate-phase3-samples.ts, configs/harness/sample-harness.yaml</read_first>
  <action>Refactor `parseHarnessConfig()` in `packages/adapter-maestro/src/harness-config.ts` so missing `configs/harness/sample-harness.yaml` no longer returns `buildDefaultHarnessConfig()` silently. Keep explicit parsing/validation behavior, but throw a clear error that names `configs/harness/sample-harness.yaml` and explains that the canonical harness config is required for the repo baseline. Remove any now-dead default-config builder logic if it is no longer used.</action>
  <verify>rg -n "buildDefaultHarnessConfig|Missing.*sample-harness.yaml|required" packages/adapter-maestro/src/harness-config.ts</verify>
  <acceptance_criteria>
    - `packages/adapter-maestro/src/harness-config.ts` does not return `buildDefaultHarnessConfig()` from the ENOENT path
    - `packages/adapter-maestro/src/harness-config.ts` contains an error message that includes `configs/harness/sample-harness.yaml`
  </acceptance_criteria>
  <done>Harness loading fails loudly instead of silently changing the runtime contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused tests for canonical harness-config policy</name>
  <files>packages/adapter-maestro/test/harness-config.test.ts</files>
  <read_first>packages/adapter-maestro/src/harness-config.ts, packages/adapter-maestro/test/recording-runtime.test.ts, packages/adapter-maestro/test/ui-runtime.test.ts</read_first>
  <action>Create `packages/adapter-maestro/test/harness-config.test.ts` using `node:test` and `node:assert/strict`. Add one test that verifies `parseHarnessConfig()` throws when the target config file is missing, and one test that verifies `loadHarnessSelection()` can still read `configs/harness/sample-harness.yaml` and resolve a real configured profile such as `flutter_android` or `native_ios` with non-empty `runner_script`, `app_id`, and `flows` values.</action>
  <verify>pnpm --filter @mobile-e2e-mcp/adapter-maestro test</verify>
  <acceptance_criteria>
    - `packages/adapter-maestro/test/harness-config.test.ts` exists
    - `packages/adapter-maestro/test/harness-config.test.ts` contains `parseHarnessConfig`
    - `packages/adapter-maestro/test/harness-config.test.ts` contains `loadHarnessSelection`
  </acceptance_criteria>
  <done>Repo-owned config behavior is covered by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @mobile-e2e-mcp/adapter-maestro test`
- [ ] `pnpm typecheck`
- [ ] `git status --short` shows only intended plan changes
</verification>

<success_criteria>

- All tasks completed
- Harness config no longer falls back silently on missing canonical config
- Canonical config paths are trackable by git
- Tests cover the new harness-loading contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-capability-verification-productization/01-01-SUMMARY.md` using `.planning/templates/SUMMARY-TEMPLATE.md`.
</output>
