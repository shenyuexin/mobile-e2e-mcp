---
phase: 01-capability-verification-productization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/README.md
  - docs/guides/golden-path.md
  - docs/architecture/framework-coverage.md
autonomous: true
requirements:
  - CAP-01
  - DOC-01
user_setup: []
must_haves:
  truths:
    - Public-facing docs describe RN and Flutter as platform-backed profiles rather than fully separate execution backends.
    - Tool-count and support-boundary wording match the live registry and current partial/full semantics.
  artifacts:
    - README.md
    - docs/README.md
    - docs/guides/golden-path.md
    - docs/architecture/framework-coverage.md
  key_links:
    - Docs use the same capability-boundary framing as the live registry and architecture docs.
---

<objective>
Align the highest-traffic support-boundary docs with the current runtime reality.

Purpose: Reduce trust erosion caused by docs that overclaim, drift from the registry, or blur the platform-adapter/profile model.
Output: Updated README/guides/architecture docs with accurate current-state wording.
</objective>

<execution_context>
@.planning/README.md
@.planning/templates/SUMMARY-TEMPLATE.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-capability-verification-productization/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@README.md
@docs/README.md
@docs/guides/golden-path.md
@docs/architecture/framework-coverage.md
@packages/mcp-server/src/server.ts
@packages/adapter-maestro/src/capability-model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct stale or misleading high-traffic doc claims</name>
  <files>README.md, docs/README.md, docs/guides/golden-path.md</files>
  <read_first>README.md, docs/README.md, docs/guides/golden-path.md, packages/mcp-server/src/server.ts, .planning/codebase/CONCERNS.md</read_first>
  <action>Update the top-level docs so they stop implying unsupported parity or stale counts. Specifically: make the tool count in `docs/guides/golden-path.md` consistent with the live registry in `packages/mcp-server/src/server.ts`; fix or remove dead doc references in `docs/README.md`; and tighten any README wording that reads like broad cross-framework parity where the current implementation is only baseline or partial.</action>
  <verify>rg -n "54 tools|p0-p2-execution-plan|React Native|Flutter|partial|55 tools" README.md docs/README.md docs/guides/golden-path.md</verify>
  <acceptance_criteria>
    - `docs/guides/golden-path.md` contains `55` for the current tool count or avoids hard-coding a stale count
    - `docs/README.md` does not reference a non-existent `p0-p2-execution-plan.zh-CN.md`
  </acceptance_criteria>
  <done>Reader-facing docs no longer advertise known-stale or missing references.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize the platform-backbone plus framework-profile explanation</name>
  <files>README.md, docs/architecture/framework-coverage.md</files>
  <read_first>README.md, docs/architecture/framework-coverage.md, docs/architecture/framework-coverage.md, configs/profiles/react-native.yaml, configs/profiles/flutter.yaml, packages/contracts/src/types.ts</read_first>
  <action>Make the framework model explicit in both README-level and architecture-level language: Android/iOS remain the execution backbone; React Native and Flutter are framework profiles or lanes layered on top. Preserve the Mermaid diagram in `docs/architecture/framework-coverage.md`, and ensure any summary wording in `README.md` does not imply RN/Flutter each have a separate fully pluggable backend today.</action>
  <verify>rg -n "framework profile|platform adapter|execution backbone|React Native|Flutter" README.md docs/architecture/framework-coverage.md</verify>
  <acceptance_criteria>
    - `docs/architecture/framework-coverage.md` contains `platform-backbone plus framework-profile`
    - `README.md` contains wording that distinguishes platform execution from framework profile support
  </acceptance_criteria>
  <done>The most visible docs tell the same architectural truth about framework support.</done>
</task>

<task type="auto">
  <name>Task 3: Re-read for support-boundary consistency after edits</name>
  <files>README.md, docs/README.md, docs/guides/golden-path.md, docs/architecture/framework-coverage.md</files>
  <read_first>README.md, docs/README.md, docs/guides/golden-path.md, docs/architecture/framework-coverage.md, packages/adapter-maestro/src/capability-model.ts</read_first>
  <action>Do a final doc-consistency pass across the four edited files. Remove wording that would claim full parity for iOS Flutter, general RN debugger parity, or unsupported enterprise/governance features in the context of current repository support. Keep the edits small and high-signal.</action>
  <verify>rg -n "parity|fully supported|enterprise|approval workflow|compliance export" README.md docs/README.md docs/guides/golden-path.md docs/architecture/framework-coverage.md</verify>
  <acceptance_criteria>
    - The edited docs do not introduce new `fully supported` claims for RN/Flutter parity
    - The edited docs do not introduce new enterprise-governance claims in these files
  </acceptance_criteria>
  <done>Support-boundary wording is consistent after the edit set.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `git diff -- README.md docs/README.md docs/guides/golden-path.md docs/architecture/framework-coverage.md` reads as support-boundary alignment only
</verification>

<success_criteria>

- All tasks completed
- High-traffic docs match the live registry and architecture model
- No stale references or overclaims remain in the edited files
</success_criteria>

<output>
After completion, create `.planning/phases/01-capability-verification-productization/01-02-SUMMARY.md` using `.planning/templates/SUMMARY-TEMPLATE.md`.
</output>
